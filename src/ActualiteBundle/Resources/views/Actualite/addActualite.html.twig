{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="">
                <div class="form-group">


                    {{ form_start(form) }}
                    {# ... #}

                    {{ form_row(form.image) }}

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}